package net.pouyan.screenshotassistant.config;

/**
 * Colour style of the "Screenshot saved" HUD notification text.
 */
public enum MessageColorMode {
    /** Cycles through red → gold → yellow → green → aqua → blue → purple. */
    RAINBOW,
    GOLD,
    GREEN,
    AQUA,
    LIGHT_PURPLE,
    WHITE
}
